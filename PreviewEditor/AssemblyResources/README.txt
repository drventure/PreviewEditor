This folder is specifically for embedding the DLLs
that are used by this project so that they don't have to be distributed
as separate files.

All files in this folder should be embedded as resources
and should have their filename cases preserved to match exactly.

For more, see the PreviewEditorHandler.cs file and the assembly load overriding.